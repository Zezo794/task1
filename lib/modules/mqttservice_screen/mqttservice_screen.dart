import 'package:flutter/material.dart';

class MqttServiceScreen extends StatelessWidget {
  const MqttServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(child: Text('hello'))
      ],
    );
  }
}
